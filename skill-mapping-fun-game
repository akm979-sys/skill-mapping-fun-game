/* CSS to improve overflow handling */
.question {
    overflow: auto;
    max-height: 500px; /* Adjust this value as needed */
}

/* JavaScript to improve rendering of questions */
document.querySelectorAll('.question').forEach(question => {
    question.style.display = 'block';
});
